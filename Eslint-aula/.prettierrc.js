Module.exports = { /* pra executar automaticamente as funcões que for determinada abaixo. */
  semi: true, /* imprime o ponto e virgula no final de todas as linhas. */ 
  trailingComma: "all", /* É o virgula. */
  singleQuote: true, /* Para utilizar aspas duplas. */ 
  printWidth: 100, /* maximo de caracteres em uma linha. */
  tabWidth: 2, /* apertar tab pula duas linhas. */
};
